{
  "name": "lumen-support",
  "version": "1.0.0",
  "private": true,
  "description": "Lumen Support — landing page + embedded multilingual AI chat widget, backed by Vercel serverless functions (Hugging Face for chat, Razorpay + email/Telegram for escalation).",
  "engines": { "node": ">=18" },
  "scripts": {
    "build": "echo 'No build step needed — plain static site'",
    "deploy": "vercel --prod"
  },
  "dependencies": {
    "nodemailer": "^6.9.14"
  }
}
